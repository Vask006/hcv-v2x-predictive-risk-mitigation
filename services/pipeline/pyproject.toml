[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "hcv-pipeline"
version = "0.1.0"
description = "Local orchestration for camera, GPS, and risk engine services"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
package-dir = { "" = "src" }
py-modules = ["event_pipeline", "pipeline_runner"]
