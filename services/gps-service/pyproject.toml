[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "hcv-gps-service"
version = "0.1.0"
description = "NMEA GNSS reader service for edge telemetry ingestion"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
serial = ["pyserial>=3.5"]

[tool.setuptools]
package-dir = { "" = "src" }
py-modules = ["gps_models", "gps_reader", "gps_service"]
