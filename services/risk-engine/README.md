# risk-engine

Rule-based risk scoring engine for normalized edge observations and context signals.

## Run tests

```bash
cd services/risk-engine
python -m pytest tests -q
```

## Usage

Use this service through `services/pipeline` or import `RiskEngine` directly from `src/risk_engine.py`.
