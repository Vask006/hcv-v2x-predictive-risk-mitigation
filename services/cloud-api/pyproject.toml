[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "hcv-cloud-api-adapters"
version = "0.1.0"
description = "Cloud ingest adapter and API client package"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
package-dir = { "" = "src" }
py-modules = ["adapter", "client"]
