# Telemetry Service

## Purpose

Normalizes raw edge inputs from camera, GPS, and future vehicle telemetry into a shared contract for downstream processing.

## Phase 1 Role

- standardize event schema
- validate required fields
- forward normalized events to the risk engine and cloud API
